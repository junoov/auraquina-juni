# Debug Journal — admin order detail activity log
Started: 2026-07-16T13:40:00+07:00
Goal: Make the recent-order "Buka pesanan" action render the Filament order detail without HTTP 500.

## Environment snapshot
- Runtime: PHP 8.3+, Laravel 13.8, Filament 5.x-dev, served by `php artisan serve` on 127.0.0.1:8000.
- Repro: authenticated owner opens `/admin/pesanans/13`; browser receives 500 at `activity-log.blade.php:12` with incomplete Eloquent Collection.
- References read: debugging methodology setup, investigation, fix, QA, cleanup; Playwright CLI.

## Hypotheses
1. [OPEN] Livewire/Filament cannot hydrate an Eloquent Collection used directly as custom TextEntry state. Evidence: failing page state names `Illuminate\\Database\\Eloquent\\Collection` as incomplete object. If true, fix is: plain arrays.
2. [OPEN] Cached Eloquent Collection loses class metadata between requests. Evidence: uncached return works while cached return reproduces. If true, fix is: cache arrays.
3. [OPEN] Blade assumes `Collection`, but Filament supplies a serialized array after hydration. Evidence: `$getState()` runtime type differs from Collection. If true, fix is: normalize state.

## Artifacts to revert
- [ ] `.debug-journal.md` — remove after verification.
- [ ] Regression test added for order detail — keep as part of final fix.
- [ ] Source fix in activity-log state/view — keep as part of final fix.

## Findings
- QA reviewer reproduced HTTP 500 at `resources/views/filament/admin/infolists/activity-log.blade.php:12` while opening the new recent-order view action.

## Final fix
Fix applied. `PesananInfolist::activityLog` now returns raw arrays. Blade template casts array back to a simple Support Collection and renders. Tests pass. 
Ready to close the current bug fix.
